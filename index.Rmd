---
title: "Methods for Supporting Decisions under Uncertainty"
author: "Cory Whitney"
date: "`r format(Sys.Date(), '%B %d, %Y')`"
bibliography: 
  - bib/decisions.bib
  - bib/packages.bib
  
  - bib/01_1_review_synthesis.bib
  - bib/01_Introduction.bib
 
output: 
  html_document:
    toc: true
    toc_depth: 3
    theme: flatly
    highlight: textmate
    df_print: kable
---

```{r knitr_setup, include=FALSE, message=FALSE, warning=FALSE}

# Global knitr options
knitr::opts_chunk$set(
  echo = TRUE,           # Show code in output
  eval = TRUE,           # Evaluate code chunks
  include = TRUE,        # Include chunk output
  message = FALSE,       # Suppress package messages
  warning = FALSE,       # Suppress warnings
  error = FALSE,         # Continue on errors (set to TRUE for debugging)
  cache = TRUE,          # Cache results for faster knitting
  cache.lazy = FALSE,    # Eager caching for large datasets
  fig.width = 10,        # Figure width in inches
  fig.height = 6,        # Figure height in inches
  fig.align = "center",  # Figure alignment
  fig.pos = "H",         # Figure position
  out.width = "90%",     # Output width
  fig.cap = TRUE,        # Enable figure captions
  dev = "png",           # Graphics device
  dpi = 300,             # Figure resolution
  tidy = FALSE,          # Don't tidy code (preserve formatting)
  results = "markup"     # How to display results
)


# Load required packages quietly
suppressPackageStartupMessages({
  library(bib2df)        # BibTeX file processing
  library(bibtex)
  library(data.table)
  library(dplyr)         # Data manipulation
  library(tidyr)         # Data tidying
  library(stringr)       # String operations
  library(purrr)         # Functional programming
  library(ggplot2)       # Data visualization
  library(scales)        # Plot scales
  library(kableExtra)    # Enhanced tables
  library(knitr)
  library(magrittr)
  library(DT)            # Interactive tables
  library(qs2)
  library(networkD3)
  library(tidytext)
  library(ggthemes)      # Additional ggplot themes
})

# Generate bibliography for all used packages
packages_to_cite <- c("base", 
                      "bib2df", 
                      "bibtex",
                      "data.table",
                      "dplyr", 
                      "tidyr", 
                      "stringr", 
                      "purrr", 
                      "ggplot2", 
                     "knitr", 
                     "magrittr",
                     "bookdown", 
                     "qs2",
                     "qs",
                     "tidytext",
                     "networkD3",
                     "DT")
knitr::write_bib(packages_to_cite, "bib/packages.bib")

```

```{r child="01_Introduction.Rmd"}
```

```{r child="02_Review.Rmd"}
```

```{r child="03_Data_Import_Pre_Processing.Rmd"}
```

# Method Extraction and Synthesis

We use Keeney’s representation of where formal decision support is required and applied as motivation and framework for the assessment - Keeney’s personal histogram of 10,000 decisions being faced by numerous decision makers [@keeneyMakingBetterDecision2004b].

![Keeney’s judgment about how 10,000 decisions are typically made - his personal histogram of 10,000 decisions being faced by numerous decision makers](figs/fig01_Keeney.png){width=50%}

# Automated Method Detection

Apply automated classification using a custom dictionary-based algorithm `detect_methods.R` identifying methods from text content with `purrr` package [@R-purrr]. Methods appearing in titles receive higher reliability weights for subsequent analysis. Create `clean_bib_data` by adapting `clean_bib_data_prisma` with the `mutate` function.

```{r method_categories, echo=FALSE}
# Method categories

method_categories <- list(
  # Core decision structuring)
  "Decision Analysis" = c(
    # Formal decision analysis
    "decision analysis", "decision analytic", "decision tree", "influence diagram", 
    "decision framework", "decision support framework",
    # Value-focused
    "value of information", "voi", "value of perfect information", "vopi",
    "value of partial perfect information", "voppi", "expected value",
    "expected value of perfect information", "evpi",
    # Decision rules
    "maximin", "maximax", "minimax regret", "hurwicz", "laplace",
    # Advanced DA
    "markov decision process", "mdp", "partially observable markov decision process", "pomdp",
    "dynamic decision analysis", "sequential decision", "real options analysis"
  ),
  
  # Quantifying and managing uncertainty)
  "Uncertainty & Risk Analysis" = c(
    # Risk analysis
    "risk analysis", "risk assessment", "risk management", "risk characterization",
    "probabilistic risk assessment", "pra", "quantitative risk assessment", "qra",
    # Uncertainty analysis
    "uncertainty analysis", "uncertainty quantification", "uncertainty propagation",
    "epistemic uncertainty", "aleatory uncertainty", "deep uncertainty",
    "robust decision making", "info-gap", "info gap",
    # Sensitivity analysis
    "sensitivity analysis", "global sensitivity analysis", "local sensitivity",
    "sensitivity testing", "parameter sensitivity", "tornado diagram",
    "sobol", "morris method", "fast", "e-fast",
    # Scenario analysis
    "scenario analysis", "scenario planning", "scenario development",
    "multiple futures", "alternative futures"
  ),
  
  # Numerical/computational approaches)
  "Computational Modeling" = c(
    # Simulation methods
    "simulation", "monte carlo", "monte-carlo", "stochastic simulation",
    "discrete event simulation", "des", "agent-based model", "abm", "multi-agent",
    "system dynamics", "system dynamic", "continuous simulation",
    "microsimulation", "micro-simulation", "individual-based model",
    "markov model", "state-transition model", "cohort model",
    # Numerical methods
    "numerical analysis", "numerical method", "finite element", "finite difference",
    "computational model", "computer model", "digital twin"
  ),
  
  # Data-driven inference)
  "Statistical Methods" = c(
    # Bayesian methods
    "bayesian", "bayes", "mcmc", "markov chain monte carlo", "gibbs sampling",
    "bayesian hierarchical", "bayesian network", "belief network",
    "bayesian belief network", "bn", "bayesian model averaging",
    "prior distribution", "posterior distribution", "conjugate prior",
    # Frequentist methods
    "regression", "linear regression", "logistic regression", "cox regression",
    "generalized linear model", "glm", "mixed model", "multilevel model",
    "time series", "arima", "arch", "garch", "vector autoregression", "var",
    "survival analysis", "kaplan-meier", "cox proportional hazards",
    "meta-analysis", "systematic review", "meta regression",
    # Experimental design
    "design of experiments", "doe", "factorial design", "response surface",
    "optimal design", "adaptive design"
  ),
  
  # Finding optimal solutions)
  "Optimization" = c(
    # Mathematical programming
    "optimization", "mathematical programming", "linear programming", "lp",
    "integer programming", "mixed integer programming", "mip",
    "nonlinear programming", "nlp", "quadratic programming", "qp",
    "dynamic programming", "stochastic programming", "robust optimization",
    "multi-objective optimization", "pareto optimization", "pareto front",
    # Heuristics & metaheuristics
    "heuristic", "metaheuristic", "genetic algorithm", "ga", "evolutionary algorithm",
    "simulated annealing", "tabu search", "particle swarm", "ant colony",
    # Constraint programming
    "constraint programming", "constraint satisfaction", "sat solver"
  ),
  
  # Handling multiple objectives)
  "Multi-Criteria Analysis" = c(
    # MCDA methods
    "multi-criteria", "multi criteria", "multiattribute", "multi-attribute",
    "multicriteria decision analysis", "mcda", "multiple criteria",
    "analytic hierarchy process", "ahp", "analytic network process", "anp",
    "topsis", "technique for order preference by similarity", 
    "promethee", "preference ranking organization method", 
    "electre", "elimination and choice expressing reality",
    "multi-objective decision analysis", "moda",
    # Value/utility theory
    "multiattribute utility theory", "maut", "multiattribute value theory", "mavt",
    "utility function", "value function", "preference elicitation",
    "swing weighting", "trade-off analysis"
  ),
  
  # Data-driven pattern recognition)
  "Machine Learning & AI" = c(
    # Supervised learning
    "machine learning", "ml", "neural network", "nn", "deep learning",
    "convolutional neural network", "cnn", "recurrent neural network", "rnn",
    "random forest", "support vector machine", "svm", "gradient boosting",
    "xgboost", "lightgbm", "catboost",
    # Unsupervised learning
    "clustering", "k-means", "hierarchical clustering", "dbscan",
    "dimensionality reduction", "pca", "t-sne", "umap",
    # Reinforcement learning
    "reinforcement learning", "rl", "q-learning", "deep q-network", "dqn",
    "policy gradient", "actor-critic",
    # Natural language processing
    "natural language processing", "nlp", "text mining", "topic modeling"
  ),
  
  # Economic assessment methods)
  "Economic Evaluation" = c(
    # Cost analysis
    "economic evaluation", "cost-effectiveness", "ce", "cost-benefit", "cba",
    "cost-utility", "cua", "cost-minimization", "cma",
    # Health economics
    "quality-adjusted life year", "qaly", "disability-adjusted life year", "daly",
    "incremental cost-effectiveness ratio", "icer",
    "budget impact analysis", "bia", "affordability",
    # Advanced economic methods
    "contingent valuation", "willingness to pay", "wtp",
    "discrete choice experiment", "dce", "stated preference",
    "revealed preference", "hedonic pricing"
  ),
  
  # Stakeholder engagement)
  "Participatory Methods" = c(
    # Expert elicitation
    "expert elicitation", "expert judgment", "expert opinion",
    "delphi method", "delphi technique", "modified delphi",
    "nominal group technique", "ngt",
    # Stakeholder engagement
    "stakeholder analysis", "stakeholder engagement", "stakeholder participation",
    "stakeholder deliberation", "participatory modeling",
    # Consensus methods
    "consensus development", "consensus conference", "consensus panel",
    "structured decision making", "sdm", "collaborative decision making"
  ),
  
  # Holistic/whole-system approaches)
  "Systems Analysis" = c(
    "systems analysis", "systems thinking", "system analysis",
    "soft systems methodology", "ssm",
    "viable system model", "vsm",
    "critical systems thinking", "cst",
    "wicked problems", "complex systems", "complex adaptive systems"
  ),
  
  # Psychology of decision making)
  "Behavioral Decision Science" = c(
    "behavioral decision", "cognitive bias", "heuristics and biases",
    "prospect theory", "expected utility", "subjective expected utility",
    "bounded rationality", "satisficing",
    "nudge", "choice architecture", "libertarian paternalism"
  ),
  
  # Decision communication)
  "Visualization & Communication" = c(
    "decision dashboard", "interactive visualization", "visual analytics",
    "geographic information system", "gis", "spatial analysis",
    "data visualization", "infographic", "storytelling",
    "uncertainty visualization", "probability visualization"
  )
)


# Enhanced detection function
detect_methods <- function(text) {
  if(is.na(text) || text == "") return("")
  
  text_lower <- tolower(text)
  methods_detected <- character()
  
  for(method in names(method_categories)) {
    keywords <- method_categories[[method]]
    # Use word boundaries to avoid partial matches
    patterns <- paste0("\\b", keywords, "\\b")
    if(any(str_detect(text_lower, patterns))) {
      methods_detected <- c(methods_detected, method)
    }
  }
  
  return(paste(unique(methods_detected), collapse = "; "))
}

# Apply method detection
clean_bib_data_methods <- clean_bib_data_prisma %>%
  mutate(
    detected_methods = map_chr(full_text, detect_methods),
    methods_from_title = map_chr(TITLE, detect_methods)
  )
```

After cleaning `r sum((clean_bib_data_methods$detected_methods != "")==TRUE)` papers had some methods detected.

```{r full_text}
# Ensure full_text exists in the filtered data
if(!"full_text" %in% names(clean_bib_data_methods)) {
  clean_bib_data_methods <- clean_bib_data_methods %>%
    mutate(full_text = paste(TITLE, ANNOTE, sep = " "))
}
```

`r length(clean_bib_data_methods$full_text)` papers have a full text for further analysis. 

## Method Frequency Analysis

Quantify and visualize the distribution of methods in the bib using frequency and bar chart from `ggplot2` [@R-ggplot2].

```{r method_results_plot, echo=FALSE}
# Method frequency analysis
analyze_method_frequency <- function(data) {
  method_counts <- data %>%
    filter(detected_methods != "") %>%
    separate_rows(detected_methods, sep = "; ") %>%
    count(detected_methods, sort = TRUE)
  
  return(list(counts = method_counts))
}

method_results <- analyze_method_frequency(clean_bib_data_methods)

kable(method_results$counts, caption = "Counts of methods used")

```

## Papers Summary

Summarize all papers with `summarise` from [@R-dplyr] and print with `knitr` [@R-knitr]. 

```{r add_confidence_scores, echo=FALSE}
# Add confidence scores for method detection
add_confidence_scores <- function(data) {
  data %>%
    mutate(
      # Confidence based on where method was detected
      method_confidence = case_when(
        methods_from_title != "" & detected_methods != "" ~ "high",
        methods_from_title != "" ~ "medium",
        detected_methods != "" ~ "medium",
        TRUE ~ "low"
      ),
      
      # Number of methods detected
      method_count = str_count(detected_methods, ";") + 1,
      method_count = ifelse(detected_methods == "", 0, method_count)
    )
}

clean_bib_data_confidence <- add_confidence_scores(clean_bib_data_methods)

# Create summary report
summary_stats <- clean_bib_data_confidence %>%
  summarise(
    total_papers = n(),
    papers_with_methods = sum(detected_methods != ""),
    high_confidence = sum(method_confidence == "high")
  )

knitr::kable(summary_stats, caption = "Summary of our confidence in the detection of methods applied in the selected papers")
```

## Dominant Methods

Count prevalent methods using `count` for frequency analysis and `tidyr` [@R-tidyr] for data restructuring.

```{r top_methods, echo=FALSE}
# Show top methods
top_methods <- clean_bib_data_confidence %>%
  filter(detected_methods != "") %>%
  separate_rows(detected_methods, sep = "; ") %>%
  count(detected_methods, sort = TRUE) %>%
  head(10)

kable(top_methods, caption = "Top 10 methods applied across all papers")
```

`top_methods` reveals the method ecosystem's core components, highlighting established practices and potential gaps in decision support under uncertainty literature.

## VOI Method Validation

Examine Value of Information (VOI) papers specifically to assess classification plausibility and confidence distribution within this focused method domain.

```{r validation_check, echo=FALSE}
# Quick validation - check if our detection makes sense

validation_check <- clean_bib_data_confidence %>%
  select(TITLE, detected_methods, method_confidence) %>%
  arrange(desc(method_confidence))

kable(table(validation_check$detected_methods), caption = "Validate detected methods")
```

`validation_check` ensures classification accuracy.

# High-Reliability Classifications

Examine the subset of classifications receiving the highest confidence rating, where methods were detected in both titles and full text.

```{r high_confidence, echo=FALSE}
# Check papers with high confidence

high_confidence <- clean_bib_data_confidence %>%
  filter(method_confidence == "high") %>%
  select(TITLE, detected_methods, methods_from_title)

kable(high_confidence, caption = "Papers with high confidence")
```

## Hierarchy of decision intensity

Keeney's concept of a "hierarchy of decision intensity" mapped to method sophistication in the literature.

Plot with `networkD3` [@R-networkD3]. 

```{r plot_sankey, echo=FALSE}
source("R/plot_sankey.R")

method_counts <- clean_bib_data_confidence %>%
  filter(detected_methods != "") %>%
  count(detected_methods) %>%
  arrange(desc(n))

# Take top 10 method categories for clarity
top_methods <- method_counts %>%
  head(10)  # Adjust number as needed

# Prepare data
total_papers <- sum(method_counts$n)

# Inputs
inputs = c(total_papers, 0)

# Losses: Counts for top methods
losses = top_methods$n

# Labels
labels = c(
  "All Papers",
  "",
  top_methods$detected_methods  # Actual method names
)

# Create Sankey (might need adjustment for long labels)
plot_sankey(inputs, losses, "Papers", labels)

# For better visualization with long labels, consider abbreviating:
abbreviated_labels <- c(
  "All Papers",
  "",
  # Abbreviate method names
  sapply(top_methods$detected_methods, function(x) {
    if(nchar(x) > 30) {
      paste0(substr(x, 1, 27), "...")
    } else {
      x
    }
  })
)

plot_sankey(inputs, losses, "Papers", abbreviated_labels)

```

```{r child="References.Rmd"}
```
